catalog:
    default:
        uri: http://iceberg-catalog:8181
        s3.endpoint: http://minio:9000
        # Local development defaults — match MINIO_ROOT_USER/PASSWORD in .env.example
        # Update these if you change MinIO credentials in your .env
        s3.access-key-id: admin
        s3.secret-access-key: password
